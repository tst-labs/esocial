version: "3.5"
services:
  db:
    image: postgres:10
    container_name: esocial-db
    volumes:
      - "/var/lib/postgresql/data"
    ports:
      - "5432:5432"
    networks:
      - esocial-net
    environment:
      - POSTGRES_PASSWORD=esocial

networks:
  esocial-net:
    driver: bridge
